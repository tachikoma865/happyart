# Instagram 自動投稿セットアップ手引き（初心者向け・全部入り）

このスクリプト（`instagram_auto_poster.py` / `instagram_connection_test.py`）を動かすには、
Metaから **2つの鍵** を取得して `config.json` に貼り付けます。

- `ACCESS_TOKEN` … Metaの長期アクセストークン（60日有効）
- `INSTAGRAM_BUSINESS_ACCOUNT_ID` … あなたのIGビジネスアカウントのID（数字）

所要時間は初回で30〜60分ほど。あわてず1ステップずつ進めてください。
**分からなくなったら、そのステップ番号を私に伝えれば、その場面を一緒に進めます。**

---

## 事前に用意するもの

- パソコン（スマホだけだと難しいのでPC推奨）
- Facebookアカウント（なければ無料で作成）
- あなたのInstagramアカウント

---

## フェーズ0：前提を整える（ここが未完了だと先に進めません）

Instagram Graph APIは **個人アカウントでは使えません**。次の3つが必須です。

1. **IGを「プロアカウント」に切り替える**
   - Instagramアプリ → 設定 → 「アカウントの種類とツール」→「プロアカウントに切り替える」
   - 種類は「ビジネス」または「クリエイター」を選ぶ
2. **Facebookページを持つ**（個人のFBアカウントとは別物。無料）
   - facebook.com → メニュー →「ページ」→「新しいページを作成」
3. **IGとFacebookページを連携する**
   - Instagramアプリ → 設定 →「ビジネス/クリエイター」→「Facebookをリンク」から、上で作ったページと接続

> ✅ チェック：この3つが済んでいるか分からない場合は、私に「フェーズ0を確認したい」と言ってください。一緒に確認します。

---

## フェーズ1：Meta開発者アプリを作る

1. ブラウザで **https://developers.facebook.com** を開く
2. 右上の「ログイン」→ 自分のFacebookでログイン
3. 初回は「開発者として登録」を求められるので画面に従って登録
4. 上部メニュー「マイアプリ」→「アプリを作成」
5. アプリタイプは **「ビジネス」** を選択 →「次へ」
6. アプリ名（例：`happyart-poster`）と連絡先メールを入力して作成

---

## フェーズ2：アクセストークン（短期）を取得する

1. 作ったアプリの画面 → 上部「ツール」→ **「GraphAPI エクスプローラー（Graph API Explorer）」**
2. 右側の「Meta App」で自分のアプリを選択
3. 「User or Page」→ **「User Token（ユーザートークン）」** を選ぶ
4. 「Permissions（権限）」の欄に、次の5つを追加（検索して✓を入れる）：
   - `instagram_basic`
   - `instagram_content_publish`
   - `pages_show_list`
   - `pages_read_engagement`
   - `business_management`
5. 青い **「Generate Access Token（アクセストークンを生成）」** を押す
6. Facebookのログイン確認が出るので、**すべて許可**（ページとIGにチェックが入っているか確認）
7. 画面上部に長い文字列（=短期トークン）が表示される。これはまだ有効期限1時間の仮のものです。

---

## フェーズ3：長期トークン（60日）に変換する

1. 上部「ツール」→ **「アクセストークンデバッガー（Access Token Debugger）」**
2. さっきの短期トークンを貼り付けて「Debug（デバッグ）」を押す
3. 画面の一番下にある **「Extend Access Token（アクセストークンを延長）」** ボタンを押す
4. 出てきた新しい長い文字列が **長期トークン**。これが `ACCESS_TOKEN` になります。コピーしておく。

> ⚠️ 長期トークンは **60日で失効** します。切れる前に同じ手順で取り直すか、あとで自動更新の仕組みを私が用意できます（一旦は手動でOK）。

---

## フェーズ4：Instagram ビジネスアカウントID を取得する

1. 「Graph API エクスプローラー」に戻る
2. トークン欄に上の長期トークンが入った状態で、上のリクエスト欄に `me/accounts` と入力して「送信」
   - 返ってきた結果の中の `"id": "..."` が **Facebookページのid**（数字）。コピー。
3. 次にリクエスト欄を書き換えて送信：
   `【ページのid】?fields=instagram_business_account`
   - 例：`17841400000000000?fields=instagram_business_account`
4. 返ってきた `"instagram_business_account": { "id": "...." }` の中の数字が
   **`INSTAGRAM_BUSINESS_ACCOUNT_ID`** です。コピー。

---

## フェーズ5：config.json に貼って動作確認

1. プロジェクトフォルダの `config.json` をテキストエディタで開く
2. 次のように、取得した値で書き換える（ダブルクォート `"` の中だけ差し替え）：

```json
{
    "ACCESS_TOKEN": "ここに長期トークンを貼る",
    "INSTAGRAM_BUSINESS_ACCOUNT_ID": "ここにIDの数字を貼る",
    "GRAPH_API_VERSION": "v20.0"
}
```

3. 保存する
4. 診断を実行して連携できたか確認：
   - **Macの場合**：アプリ「ターミナル」を開く
   - フォルダに移動：`cd` と半角スペースを打ち、フォルダをターミナルにドラッグ&ドロップして Enter
   - 実行：`python3 instagram_connection_test.py` と打って Enter
5. `[OK]` が3つ並び「すべて正常」と出れば成功です 🎉

---

## よくあるつまずき

- **画像はローカルではダメ**：投稿する画像は「公開URL」が必須（前回の `local path not allowed` の原因）。画像の置き場所は別途私が用意します。
- **`instagram_content_publish` が選べない**：フェーズ0の連携が未完了のことが多いです。IG↔FBページ連携を見直し。
- **24時間で25投稿まで**：Instagram側の上限。リールやストーリーもこの枠に含まれます。
- **トークンが1時間で切れる**：フェーズ3の「延長」をやり忘れています。

---

## 次のステップ（このあと私がやること）

- 画像の公開URL化（GitHub Pages等）の設定
- 過去日程になっている投稿カレンダーの引き直し（新しいローンチ日で）
- 信頼ベースのキャプション一式
- LINE登録直後の自動あいさつメッセージ設計

**まずはフェーズ0から。今どこまで出来ているか教えてください。分からなければ「最初から一緒に」でOKです。**
